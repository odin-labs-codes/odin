# Public addresses only. The deployment signer is selected from a local Foundry keystore.
DEPLOYER=
PROTOCOL_TREASURY=

# Use a private Robinhood Chain mainnet endpoint on the command line via --rpc-url.
ROBINHOOD_RPC_URL=https://rpc.mainnet.chain.robinhood.com
